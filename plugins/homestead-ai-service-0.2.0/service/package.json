{
  "name": "homestead-ai-service",
  "version": "0.2.0",
  "private": true,
  "description": "Stateless, replaceable model-orchestration service for the Homestead AI Assistant plugin.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/auth.js && node --check src/provider.js && node --check src/providers/openai-compatible.js && node --check src/providers/ollama.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
